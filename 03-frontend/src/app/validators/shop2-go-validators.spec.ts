import { Shop2GoValidators } from './shop2-go-validators';

describe('Shop2GoValidators', () => {
  it('should create an instance', () => {
    expect(new Shop2GoValidators()).toBeTruthy();
  });
});
